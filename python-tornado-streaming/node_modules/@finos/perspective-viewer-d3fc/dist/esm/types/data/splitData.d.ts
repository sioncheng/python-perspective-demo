import { Settings } from "../types";
import { DataRow } from "@finos/perspective";
export type SplitData = {
    key: string;
    crossValue: string;
    mainValue: string | number | boolean | Date;
    row: DataRow;
};
export declare function splitData(settings: Settings, data: DataRow[]): SplitData[][];
