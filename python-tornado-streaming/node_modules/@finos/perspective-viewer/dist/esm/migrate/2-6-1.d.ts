import { ViewConfig } from "@finos/perspective";
import Semver from "./semver";
import { Options } from "../migrate";
/**
 * Migrates from 2.6.1
 * @param old
 * @param options
 * @returns
 */
export default function migrate_2_6_1(old: any, options: Options): any;
export type Config261 = ViewConfig & {
    version: Semver;
    plugin?: string;
    settings?: boolean;
    plugin_config?: any;
};
