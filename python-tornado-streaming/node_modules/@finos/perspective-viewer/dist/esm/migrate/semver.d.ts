export default class Semver {
    major: number;
    minor: number;
    patch: number;
    build?: {
        major: number;
        minor: number;
        patch: number;
    };
    constructor(ver: string | Semver);
    to_string(): string;
    gt(val: string | Semver): boolean;
    eq(val: string | Semver): boolean;
    ge(val: string | Semver): boolean;
}
